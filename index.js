/**
 * @format
 */

import { AppRegistry } from 'react-native';
import App from './App';
import { name as appName } from './app.json';

AppRegistry.registerComponent(appName, () => App);

// native-base Content,Right,Footer
// native-base
// theme path and consts -> native base theme connected

// i18n, - okay
// naming import * from 'shared/ui/...';
