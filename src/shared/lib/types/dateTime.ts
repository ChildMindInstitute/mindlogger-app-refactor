export type HourMinute = {
  hours: number;
  minutes: number;
};

export type DayMonthYear = {
  day: number;
  month: number;
  year: number;
};

export type DatesFromTo = { from: Date; to: Date };
