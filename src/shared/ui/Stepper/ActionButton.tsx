import { styled, Text } from '@tamagui/core';

const ActionButton = styled(Text, {
  name: 'ActionButton',
  fontSize: 20,
  color: '$blue',
  textAlign: 'center',
  alignSelf: 'center',
});

export default ActionButton;
