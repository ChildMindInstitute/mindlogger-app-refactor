export type MediaValue = {
  uri: string;
  fileName: string;
  size: number;
  type: string;
  fromLibrary: boolean;
};

export type MediaFile = {
  uri: string;
  type: string;
  fileName: string;
};
