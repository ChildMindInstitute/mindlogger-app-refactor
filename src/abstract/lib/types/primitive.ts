export type Point = {
  x: number;
  y: number;
};

export type StringOrNull = string | null;

export type IdName = {
  id: string;
  name: string;
};

export type AppletWithVersion = {
  appletId: string;
  version: string;
};
