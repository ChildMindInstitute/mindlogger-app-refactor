name: Automated tests
on: [pull_request]
jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install deps
        run: |
          npm install -g yarn
          yarn
      - name: Running tests
        run: yarn test
